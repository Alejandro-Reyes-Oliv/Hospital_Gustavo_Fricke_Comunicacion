{
  "name": "bot-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "// === Dev / Run ===": "",
    "dev": "nodemon --watch src --ext js,json --signal SIGTERM src/server.js",
    "start": "node src/server.js",
    "// === Webhook público (opcional) ===": "",
    "dev:public": "lt --port 8082 --subdomain hgf-bot"
  },
  "dependencies": {
    "contacto-fricke-monorepo": "file:../..",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "localtunnel": "^1.8.3",
    "nodemon": "^3.1.10"
  }
}
