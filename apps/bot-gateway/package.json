{
  "name": "bot-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "// === Dev / Run ===": "",
    "dev": "nodemon --exec \"node --env-file=./.env\" src/index.js",
    "start": "node src/server.js",
    "// === Webhook público (opcional) ===": "",
    "dev:public": "lt --port 8082 --subdomain hgf-bot",
    "tunnel": "wait-on tcp:8082 && ngrok http --authtoken <COLOCAR EL TOKEN QUE DEJE EN EL .ENV> --domain=undelightful-danyel-hymnodical.ngrok-free.dev 8082",
    "dev:tunnel": "concurrently -k -s first -n api,ngrok \"npm:dev\" \"npm:tunnel\""
  },
  "dependencies": {
    "contacto-fricke-monorepo": "file:../..",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "localtunnel": "^1.8.3",
    "nodemon": "^3.1.10"
  }
}
